package entity;

import "arith.proto";

option cc_generic_services = true;

service ArithServiceDivide {
	rpc divide (ArithRequest) returns (ArithResponse);
}

service ArithServiceMultiply {
	rpc multiply(ArithRequest) returns (ArithResponse);
}
